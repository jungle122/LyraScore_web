# 复制为 .env 后填入实际值。.env 不会被提交到 git。

# MySQL
DB_PASSWORD=your_mysql_root_password

# JWT 密钥（任意 32 字符以上的随机字符串）
JWT_SECRET=change-me-to-a-long-random-string-at-least-32-characters

# 阿里云百炼（qwen-turbo）— 阶段五才需要，未配置时 AI 模块走 mock
DASHSCOPE_API_KEY=
AI_ENABLED=false
